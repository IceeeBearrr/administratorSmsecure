import 'package:flutter/material.dart';

class Predictionmodelpage extends StatelessWidget {
  const Predictionmodelpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}